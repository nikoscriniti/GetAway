import pygame 
from os.path import join 
from os import walk # walk allows to "walk" through a folder, in terms of dictionaries, you call walk and have to insert a file path

WINDOW_WIDTH, WINDOW_HEIGHT = 1280,720 #called in the class "game"
TILE_SIZE = 64